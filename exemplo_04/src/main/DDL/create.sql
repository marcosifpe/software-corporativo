CREATE TABLE TB_USUARIO (TXT_CPF VARCHAR(14) NOT NULL UNIQUE, TXT_LOGIN VARCHAR(50) NOT NULL UNIQUE, DT_NASCIMENTO DATE, TXT_EMAIL VARCHAR(50) NOT NULL, TXT_NOME VARCHAR(255) NOT NULL, TXT_SENHA VARCHAR(20) NOT NULL, PRIMARY KEY (TXT_CPF, TXT_LOGIN))
